source             python-rns 1.4.2 (b48b96e6, openssl backend), RNS/Cryptography/PKCS7.py#pad stubbed to the identity
determinism        encode
purpose            three bytes of padding, two of which say something else; the reference removes three

source             header flags chosen by tools/gen, packed by python-rns 1.4.2 (b48b96e6, openssl backend)
determinism        encode
purpose            transport type TUNNEL, which writes the transport bit and the context flag together; the ratchet this announce carries is what the context flag already asked for

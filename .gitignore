# Build output
cmd/dump
*.o
conformance/*/target/
conformance/*/node_modules/
conformance/go-reticulum/goret
conformance/Reticulum-Go/q4ret
conformance/microReticulum/micro

# The implementations under test are cloned, not vendored.
# See conformance/README.
conformance/src/

# The BEAM writes this into the working directory when it dies, and
# cmd/check runs a harness with the repository root as its directory.
erl_crash.dump

tools/__pycache__/
report.html

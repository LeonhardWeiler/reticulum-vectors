Vendored code.

    tweetnacl.c
    tweetnacl.h

        origin    https://tweetnacl.cr.yp.to/20140427/
        fetched   2026-08-05
        sha256    02e65bc3013ff2168983365e55906bc783c4c7e0a60d8100f17bb303a17175c4  tweetnacl.c
                  43f29ad721d9927b747b0100ab4160c119e7bb180c7c98a66e4bf79d31244287  tweetnacl.h
        license   public domain
        modified  no

        used for  Ed25519 and X25519, and the SHA-512 they carry with
                  them and never call directly. Signing as well as
                  verifying, which a decoder would not need: test/sign
                  and test/ifac record a signature this program has to
                  produce in order to check it. Nothing else in the file
                  is used. Which entry points that is, is what nm says:

                      nm cmd/dump.o | grep ' U crypto'

Built without the warning flags the corpus applies to its own code, and UBSan reports left shifts of negative values in it, at tweetnacl.c:281 and tweetnacl.c:685. Both are the reference file as published.

tweetnacl declares randombytes but does not define it. dump supplies a definition that serves crypto_sign_keypair a caller-chosen seed, which is how an Ed25519 public key is derived from a given private key. dump never needs real randomness; that definition aborts if called for any other purpose.
